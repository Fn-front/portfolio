import bcrypt from 'bcrypt';
import { NextResponse } from 'next/server';

// biome-ignore lint:
export const POST = async (req: Request) => {
  try {
    // biome-ignore lint:
  } catch (error) {}
};
